# Minimal Repro

```console
# clone the repo
> cargo +nightly run
# open a file explorer window
# navigate to C:\test
error: process didn't exit successfully: `target\debug\my-winfsp.exe` (exit code: 0xc0000374, STATUS_HEAP_CORRUPTION)
```